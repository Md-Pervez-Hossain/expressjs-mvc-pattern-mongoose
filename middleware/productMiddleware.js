// const { productSchema } = require("../models/productSchema"); // Import the productSchema

// // Define the middleware
// productSchema.pre("save", function (next) {
//   if (this.quantity === 0) {
//     this.status = "out-of-stock";
//   }
//   next();
// });

// // Export the middleware as pMiddleware
// module.exports = productSchema;
